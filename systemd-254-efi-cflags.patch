diff -up systemd-261/src/boot/meson.build.1~ systemd-261/src/boot/meson.build
--- systemd-261/src/boot/meson.build.1~	2026-06-19 20:08:35.000000000 +0200
+++ systemd-261/src/boot/meson.build	2026-06-20 17:59:19.331278198 +0200
@@ -175,6 +175,7 @@ efi_c_args = [
         '-fno-strict-aliasing',
         '-fshort-wchar',
         '-include', 'efi-config.h',
+        '-fPIC',
 ]
 
 efi_c_args += cc.get_supported_arguments(
@@ -197,10 +198,11 @@ if get_option('libc') == 'musl'
 endif
 
 efi_c_ld_args = [
+        '-fPIC',
         '-nostdlib',
         '-static-pie',
         '-Wl,--entry=efi_main',
-        '-Wl,--fatal-warnings',
+        '-Wl,--no-fatal-warnings',
 
         # These flags should be passed by -static-pie, but for whatever reason the flag translation
         # is not enabled on all architectures. Not passing `-static` would just allow the linker to
@@ -243,6 +245,9 @@ efi_disabled_c_args = cc.get_supported_a
         '-fno-asynchronous-unwind-tables',
         '-fno-exceptions',
         '-fno-unwind-tables',
+        '-U_FORTIFY_SOURCE',
+        '-fno-stack-protector',
+        '-fno-stack-protector-all',
 )
 efi_override_options = [
         'b_coverage=false',
@@ -274,8 +279,8 @@ efi_arch_c_args = {
         'riscv32'     : ['-march=rv32imafdc_zicsr_zifencei'],
         'riscv64'     : ['-march=rv64imafdc_zicsr_zifencei'],
         # Pass -m64/32 explicitly to make building on x32 work.
-        'x86_64'      : ['-m64', '-march=x86-64', '-mno-red-zone', '-mgeneral-regs-only'],
-        'x86'         : ['-m32', '-march=i686', '-mgeneral-regs-only', '-malign-double'],
+        'x86_64'      : ['-m64', '-march=x86-64', '-mno-red-zone', '-mgeneral-regs-only', '-mfpmath=387', '-fno-pie', '-fPIC'],
+        'x86'         : ['-m32', '-march=i686', '-mgeneral-regs-only', '-malign-double', '-mfpmath=387', '-fno-pie', '-fPIC'],
 }
 efi_arch_c_ld_args = {
         # libgcc is not compiled with -fshort-wchar, but it does not use it anyways,
